export interface HeroServiceData {
    title: string;
  }
  
  export const RepairService_data: { heroService: HeroServiceData } = {
    heroService: {
      title: "Repair Service",     
    },
  };
  